import React from 'react';

export interface ESAppBarProps {
  officeSwitcher?: React.ReactNode;
  searchField?: React.ReactNode;
  notifications?: React.ReactNode;
  profileButton?: React.ReactNode;
}
