import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

const MenuGroupStyled = styled(Box, {
  name: 'ESMenuGroup',
  slot: 'Root',
})(() => ({}));

export default MenuGroupStyled;
