import { Breakpoints } from '@mui/system';

const breakpoints: Partial<Breakpoints> = {
  values: {
    xs: 0,
    sm: 30.625,
    md: 48,
    lg: 65,
    xl: 90,
  },
  unit: 'em',
};

export default breakpoints;
